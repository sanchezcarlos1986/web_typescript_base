import React from 'react';
import {Layout} from '../components';

const ProductsPage: React.FC = () => {
  return (
    <div>
      <Layout title="Products Page">Contenido Products</Layout>
    </div>
  );
};

export default ProductsPage;
