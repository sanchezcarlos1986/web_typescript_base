import React from 'react';
import {Layout} from '../components';

const HomePage: React.FC = () => {
  return (
    <div>
      <Layout title="Home Page">Contenido Home</Layout>
    </div>
  );
};

export default HomePage;
