import React from 'react';
import {Layout} from '../components';

const OrdersPage: React.FC = () => {
  return (
    <div>
      <Layout title="Orders Page">Contenido Orders</Layout>
    </div>
  );
};

export default OrdersPage;
